# GraphML Visualizer

Приложение для визуализации и валидации GraphML файлов графов.

## 🚀 Быстрый старт

### Требования
- Docker Desktop ([скачать](https://www.docker.com/products/docker-desktop))
- Git

### Установка и запуск

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/GiornoGiovanaJoJo/lastdz.git
cd lastdz

# 2. Запустите приложение
docker compose up
```

**Готово!** Откройте в браузере:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000/docs

## 📝 Что происходит при запуске

```
✓ Автоматическая загрузка образов Docker
✓ npm install для frontend зависимостей
✓ pip install для backend зависимостей
✓ Запуск frontend на Vite dev сервере
✓ Запуск backend на FastAPI
```

## 📁 Структура проекта

```
.
├── backend/              # FastAPI приложение
│   ├── main.py          # REST API, обработка GraphML
│   ├── requirements.txt  # Python зависимости
│   ├── Dockerfile       # Docker конфиг
│   └── sample.graphml   # Пример файла
├── frontend/            # React + Vite приложение
│   ├── src/            # React компоненты
│   ├── package.json    # Node зависимости
│   ├── Dockerfile      # Docker конфиг
│   └── index.html
├── docker-compose.yml  # Оркестрация сервисов
└── README.md
```

## 🛠️ Команды

### Запуск

```bash
# Запуск в фоновом режиме
docker compose up -d

# Просмотр логов
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f backend
docker compose logs -f frontend
```

### Остановка

```bash
# Остановить контейнеры
docker compose stop

# Удалить контейнеры и данные
docker compose down -v
```

### Пересборка

```bash
# Пересборка с обновлением зависимостей
docker compose up --build

# Полная пересборка без кэша
docker compose up --build --no-cache
```

## 🌐 API Endpoints

- `POST /upload` - Загрузка GraphML файла
- `GET /graph` - Получение данных графа
- `GET /docs` - Swagger документация

## ✨ Функционал

- ✓ Загрузка и парсинг GraphML файлов
- ✓ Визуализация графов
- ✓ Валидация структуры
- ✓ REST API
- ✓ Полная документация API
- ✓ Docker поддержка

## 🏗️ Технологии

**Backend:**
- FastAPI
- NetworkX
- Python 3.11

**Frontend:**
- React
- TypeScript
- Vite
- Node.js 18

**DevOps:**
- Docker
- Docker Compose

## 📖 Дополнительно

При запуске `docker compose up` всё устанавливается автоматически:
- Зависимости Python загружаются в backend контейнер
- npm зависимости загружаются в frontend контейнер
- Оба сервиса запускаются и готовы к работе
